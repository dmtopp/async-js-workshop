const { encryptSearchCritiera, fetchResults } = require('../api/returnDataWithErrorFirstCallbacks')

// TODO document function signatures
// TODO this is hard

const main = searchCritiera => {
  console.log('Clocking in, time to do some async work.')

  // in this example we need to transform some data before sending it to an api
  // call encryptSearchCritiera to transform the data, then post the data to an api
  // make sure to return your results!

  return null
}
